import {NgplHighlightPipe} from './ngpl-highlight.pipe';

describe('HighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new NgplHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
