import { NgplPreventKeyboardDirective } from './ngpl-prevent-keyboard.directive';

describe('PreventKeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new NgplPreventKeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
